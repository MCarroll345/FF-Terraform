apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: aws-cloudwatch-auth
  namespace: default
spec:
  podIdentity:
    provider: aws
    identityOwner: keda
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: ffmlapp-network-scaler
  namespace: default
spec:
  scaleTargetRef:
    name: ffmlapp
  minReplicaCount: 2
  maxReplicaCount: 10
  cooldownPeriod: 120
  triggers:
    - type: aws-cloudwatch
      authenticationRef:
        name: aws-cloudwatch-auth
      metricType: AverageValue
      metadata:
        namespace: AWS/EC2
        expression: "SELECT SUM(NetworkIn) FROM \"AWS/EC2\" WHERE AutoScalingGroupName = 'eks-FF-ml-6cceb8ff-8ed2-2b2e-50b3-03984bb8f384'"
        metricName: NetworkIn
        metricStatPeriod: "60"
        metricCollectionTime: "120"
        metricStat: Sum
        targetMetricValue: "100000"
        minMetricValue: "0"
        awsRegion: eu-west-1
        identityOwner: keda
